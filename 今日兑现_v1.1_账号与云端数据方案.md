# 今日兑现 v1.1｜账号与云端数据方案

版本状态：已确认方案，未开始实施  
确认日期：2026-08-29  
目标用户：以中国大陆用户为主

## 1. 版本目标

v1.1 将「今日兑现」从单设备、本地保存的 PWA，升级为可登录、可恢复个人数据的网页产品。

核心结果：用户使用邮箱和密码注册、验证、登录后，能够安全地将当前设备的任务与记录迁移至云端，并在新设备上恢复自己的数据。

## 2. 已确认范围

### 本期实现

- 邮箱 + 密码注册、验证邮箱、登录、退出登录、找回密码；
- 首次登录时迁移当前设备已有的本地数据；
- 云端保存并恢复任务、节点、专注/中断记录、奖励、约束、设置与复盘数据；
- 保留本地缓存、离线使用能力和 JSON 导出/导入；
- 在「我的」中展示账户和云同步状态；
- 以 Supabase 提供 Auth 与 Database 能力。

### 本期不实现

- 手机号验证码登录；
- 用户头像、任务附件、图片或其他文件上传；
- 实时多人协作、共享任务；
- 复杂多设备并发编辑、实时合并和冲突编辑器；
- 将飞书反馈表单/后台迁移至 Supabase；
- 付费服务、付费 AI、短信服务或自动升级套餐。

## 3. 产品流程

### 3.1 账户流程

1. 新用户输入邮箱、密码和确认密码。
2. 系统发送验证邮件；验证成功后进入产品。
3. 已注册用户使用邮箱和密码登录；正常登录不发送邮件。
4. 用户忘记密码时，通过邮箱重置密码。
5. 用户可在「我的」页退出当前设备账号。

邮箱是认证身份，不引入自定义用户名登录。未来如需要昵称，可作为用户资料展示字段，不能替代安全认证凭证。

### 3.2 现有本地数据迁移

| 场景 | 产品行为 |
| --- | --- |
| 首次登录，云端为空 | 提示“将本机数据安全同步到云端”；用户确认后上传本地数据。 |
| 登录时云端已有数据 | 数据一致或可根据已知同步基线安全判断时不重复询问；只有双端都发生不同修改时，明确让用户选择“使用云端数据”或“保留本机数据”。 |
| 登录前匿名使用 | 继续使用现有 LocalStorage / IndexedDB；不强制注册。 |
| 离线状态 | 继续本地保存，显示待同步状态；恢复网络后若当日自动同步额度可用则补传，否则等待下个自然日或用户手动同步。 |
| 导入 JSON 备份 | 仍保留原有覆盖确认；登录后由用户主动选择是否同步这份更新。 |

### 3.3 v1.1 同步规则

- 登录后，云端作为跨设备恢复来源，本地仍是即时操作与离线兜底。
- 每次有效本地保存只更新一份最新待同步版本；普通操作每个本地自然日最多自动上传一次。
- 首次迁移、恢复、真实冲突选择和用户手动“立即同步”不受每日自动同步频次限制。
- v1.1 不承诺两台设备同时编辑同一任务时的自动合并。
- 只有检测到云端版本更新且本地也有基于同一版本的未同步修改时，才提示用户选择；一次选择完成后的普通操作只进入待同步，不重复弹窗。

## 4. 技术架构

```text
GitHub ──构建部署──> Netlify ──> 今日兑现网页 / PWA
                                      │
                     本地缓存 <───────┼──────> Supabase Auth
                  (离线、即时保存)    │          (邮箱、密码、会话)
                                      └──────> Supabase Database
                                                 (个人云端数据)
```

### 4.1 Supabase 使用边界

| 能力 | v1.1 用途 | 规则 |
| --- | --- | --- |
| Auth | 邮箱注册、验证、登录、找回密码、会话 | 不使用手机号认证。 |
| Database | 个人任务状态和资料 | 每位用户只能访问自己的数据。 |
| Storage | 不启用 | 当前没有必要的用户文件场景。 |
| Edge Functions | 暂不作为首选依赖 | 仅在后续确有服务端密钥或自定义业务需求时评估。 |

### 4.2 初始数据模型

v1.1 优先采用“小而安全”的快照模型，以兼容当前已稳定运行的本地状态结构。

| 表 | 关键字段 | 用途 |
| --- | --- | --- |
| `profiles` | `id`、`created_at`、`updated_at`、`display_name` | 与 Supabase Auth 用户一一对应的非敏感资料。 |
| `user_snapshots` | `user_id`、`state` (JSONB)、`revision`、`updated_at` | 保存当前任务、节点、事件、奖励、设置、复盘等个人状态。 |

后续需要精细查询、统计或复杂冲突处理时，再按迁移脚本逐步拆分为 `tasks`、`task_nodes`、`task_events`、`rewards` 等表；不在 v1.1 预先过度设计。

### 4.3 安全设计（不可省略）

- 每个业务表启用 Row Level Security（RLS）。
- `profiles.id` 与 `user_snapshots.user_id` 必须对应 `auth.uid()`。
- 仅允许认证用户读写本人数据；匿名用户不允许读写云端任务数据。
- 浏览器仅使用 Supabase Project URL 和 publishable/anon key；该 key 可以公开，但必须配合 RLS。
- `service_role`、SMTP 密码、数据库密码等只保存于 Supabase/Netlify 的服务端机密配置中，绝不写入前端、Git 仓库、备份或 51.LA。
- 用户任务标题、专注记录和邮箱均视为个人数据；51.LA 继续只上报既有的匿名行为指标，不上报这些内容。

## 5. 邮箱服务策略与成本闸门

### 5.1 已确定原则

- 采用“邮箱 + 密码”，邮箱验证保持开启；不采用未验证邮箱直接注册。
- 正常登录不发邮件；注册验证、找回密码、修改邮箱才发邮件。
- Supabase 默认邮件服务只用于开发测试，不作为公开用户邮件通道。
- 正式公开注册前需要配置外部 SMTP。

### 5.2 成本约束

| 项目 | 当前处理 |
| --- | --- |
| Supabase | 仅考虑 Free 计划；不得主动升级或添加付费扩展。 |
| SMTP | 只评估含免费额度的服务；不购买、不绑定付费计划，除非用户单独确认。 |
| 发信域名 | 暂不购买。公开上线前如需提升 QQ、163 等邮箱送达率，再单独提出购买建议和费用。 |
| 测试 | 先使用项目所有者授权邮箱进行开发验证；公开测试前需完成真实邮箱送达测试。 |

### 5.3 上线前邮件验收

- 注册验证、登录、找回密码、邮箱更换均能正常完成；
- 至少使用 QQ 邮箱、163 邮箱、Gmail 各完成一次验证与重置测试；
- 验证邮件的发信身份、链接跳转和有效期正确；
- 发送频率、重发间隔、错误提示和反滥用措施符合预期；
- 未使用付费 SMTP、域名或套餐时，不得对外宣称已具备稳定公开注册能力。

## 6. 分阶段实施计划

### 阶段 A：方案与环境准备

1. 创建独立的 v1.1 PRD、数据模型与验收清单。
2. 用户确认 Supabase Free 项目创建、区域选择和 SMTP 测试服务选择。
3. 创建 Supabase 项目，设置 Netlify 正式站与本地开发站为 Auth 回调地址。
4. 建立数据库迁移脚本、RLS 策略和最小测试数据。

**阶段出口：** 未写入产品业务代码前，已验证未授权请求不能读取任意用户数据。

### 阶段 B：账户与数据迁移

1. 增加注册、验证、登录、退出和找回密码界面。
2. 增加会话状态与账户页。
3. 实现本地数据与空云端账户的首次迁移。
4. 实现云端存在数据时的显式选择流程。

**阶段出口：** 老用户不会因登录丢失本地任务；新设备可恢复同一账号的云端数据。

### 阶段 C：同步、回归与发布

1. 增加待同步、同步中、同步成功、同步失败状态。
2. 离线修改后恢复网络，验证数据最终写入云端。
3. 执行现有单元测试、构建、手机视口回归和 RLS 安全测试。
4. 用真实邮箱完成邮件流程验收；推送前进行敏感文件检查。

**阶段出口：** 通过账户、迁移、恢复、离线、权限和移动端验收，才允许发布 Beta。

## 7. 已知风险与应对

| 风险 | 应对 |
| --- | --- |
| Supabase Free 项目低活跃被暂停 | 保留本地数据和 JSON 导出；上线说明中告知管理员定期检查项目状态。 |
| 大陆邮箱送达率不稳定 | 不先承诺公开上线；完成 QQ、163、Gmail 实测后再决定 SMTP/域名。 |
| 本地与云端数据冲突 | v1.1 只做明确选择，不做隐式合并。 |
| RLS 配置错误导致越权 | 先写拒绝策略与安全测试，再开放任何数据表。 |
| 前端泄露高权限密钥 | 浏览器不使用 `service_role`；提交前扫描密钥与环境文件。 |
| 过早加入文件上传 | Storage 延后，避免无业务需求的隐私与配额风险。 |

## 8. 实施前仍需用户确认的事项

1. 是否授权创建一个 Supabase **Free** 项目（不添加付款方式、不升级套餐）。
2. 是否同意先以开发测试方式配置邮箱认证；公开注册前再单独确认 SMTP 服务与可能的域名费用。
3. 是否授权在下一步创建独立的 v1.1 PRD 和数据库迁移设计文件。

在以上事项逐项获得确认前，不创建第三方项目、不填写账号信息、不配置 SMTP、不写入任何 Supabase 数据，也不修改产品代码。
