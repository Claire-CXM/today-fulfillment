# 今日兑现 v1.0｜DESIGN 设计规范

版本：v1.0
日期：2026-08-29
适用范围：今日兑现手机优先 PWA 的新增与迭代界面

## 1. 设计方向

延续已确认的「浅色青绿、柔白卡片、成长花径」基线。界面应安静、温和、可开始，帮助用户把注意力放在完成一件事上，而不是操作产品本身。

禁止转为深色、赛博、强游戏化或 SaaS 仪表盘风格。奖励是完成后的温和肯定，不应抢占完成动作本身。

## 2. Token 架构

组件样式必须从“基础值 → 语义 → 组件”三层取值；组件内避免散落无含义的色值和间距。

### 2.1 基础 Token（Primitive）

```css
:root {
  --primitive-white: #FFFFFF;
  --primitive-bg: #F7FBF8;
  --primitive-ink: #102F2B;
  --primitive-muted: #667775;
  --primitive-line: #E4ECE8;
  --primitive-green: #17A879;
  --primitive-green-deep: #087657;
  --primitive-mint: #E8F8F0;
  --primitive-blue: #4E9EFF;
  --primitive-amber: #D99021;
  --primitive-danger: #C65353;
  --primitive-radius-card: 20px;
  --primitive-radius-control: 14px;
  --primitive-shadow-card: 0 10px 28px rgb(28 80 66 / 8%);
}
```

### 2.2 语义 Token（Semantic）

```css
:root {
  --color-page: var(--primitive-bg);
  --color-surface: var(--primitive-white);
  --color-text: var(--primitive-ink);
  --color-text-secondary: var(--primitive-muted);
  --color-border: var(--primitive-line);
  --color-action: var(--primitive-green);
  --color-action-pressed: var(--primitive-green-deep);
  --color-success-bg: var(--primitive-mint);
  --color-info: var(--primitive-blue);
  --color-warning: var(--primitive-amber);
  --color-danger: var(--primitive-danger);
  --radius-surface: var(--primitive-radius-card);
  --radius-interactive: var(--primitive-radius-control);
  --shadow-surface: var(--primitive-shadow-card);
}
```

### 2.3 组件 Token（Component）

| 组件 | Token 使用 | 规则 |
| --- | --- | --- |
| 页面背景 | `--color-page` | 保持轻盈留白，不使用大面积深色底。 |
| 卡片 | `--color-surface`、`--radius-surface`、`--shadow-surface` | 柔白表面、弱阴影、边界清楚。 |
| 主按钮 | `--color-action` | 用于当前页面唯一最推荐动作。 |
| 次按钮 | `--color-surface`、`--color-border` | 支持相邻可逆操作，不与主按钮竞争。 |
| 危险操作 | `--color-danger` | 仅用于“今天无法继续”等不可逆或含义重的动作。 |
| 成功状态 | `--color-success-bg` | 用于“已达成”、完成庆祝和待领取提示。 |
| 提醒说明 | `--color-warning` | 传达限制与注意事项，避免制造故障感。 |

## 3. 全局布局与可访问性

- 以 390px 宽手机为基准，320–430px 不出现横向滚动、裁切或不可点击区域。
- 底部导航固定为：今天、日历、复盘、奖惩、我的；当前项必须同时有颜色与文字/形状区分。
- 页面主内容保留舒适边距；卡片之间使用稳定的间距节奏，不堆叠无意义分隔线。
- 所有可交互控件有效触控范围不小于 44 × 44px；设置列表单行高度不小于 56px。
- 图标不能单独承载关键信息；图标按钮必须有 `aria-label`，键盘焦点必须清晰可见。
- 对话框打开后焦点进入对话框，Esc/取消可返回原页面；危险操作要有明确取消按钮。

## 4. v1.0 核心组件规范

### 4.1 新手引导

| 项目 | 规范 |
| --- | --- |
| 触发 | 仅无历史数据的新用户首次打开时展示。 |
| 结构 | 三步短句：选一件事 → 开始专注 → 完成一次兑现。 |
| 操作 | 主按钮「创建第一件任务」；次按钮「先看看」。 |
| 视觉 | 轻量插图/进度提示可用，但不能遮蔽文字或拉长首次流程。 |
| 关闭 | 跳过后不重复强制弹出。 |

### 4.2 今天页的首次兑现状态

- 在完成概况附近展示简洁状态：`今日首次兑现 · 待完成` 或 `今日首次兑现 · 已达成`。
- 待完成使用中性文字与低饱和提示；已达成使用柔和的成功背景和绿色文字。
- 只表达当天最小目标，不展示排名、连胜压力或惩罚暗示。

### 4.3 新建任务与快捷开始

| 元素 | 规范 |
| --- | --- |
| 时长快捷项 | 15 / 25 / 45 / 60 分钟；未选中为描边，选中为青绿色强调，文本必须清晰。 |
| 节点说明 | 明确标示“可选”；说明适用于超过 45 分钟或多步骤任务，并给出“阅读 → 练习 → 复盘”示例。 |
| 主按钮 | 「保存并开始」，位于易拇指触达区域。 |
| 次按钮 | 「只加入今天」，视觉弱于主按钮但同样可点击。 |
| 取消 | 低干扰文本/描边样式，不可与保存操作混淆。 |

### 4.4 专注页的渐进披露

专注页只把当前最常用、最有助于完成的操作放在第一层。

| 层级 | 元素 | 样式与文案 |
| --- | --- | --- |
| 第一层 | 完成任务 | 唯一主按钮；绿色实心。 |
| 第一层 | 短暂休息 | 次按钮；清晰说明是临时停顿。 |
| 第二层 | 更多处理方式 | 可展开的低干扰入口。 |
| 第二层 | 遇到突发情况 | 中性/警示语气；说明会保留任务以便补做。 |
| 第二层 | 今天无法继续 | 危险语义；进入确认步骤后才执行。 |

不得继续使用“普通暂停”“特殊中断”“主动放弃”等内部式或评判性术语。

### 4.5 完成与奖励

- 任务完成后先展示一次轻量的完成反馈；避免同时出现多个弹窗、Toast 和奖励选择。
- 不自动打开领奖弹窗。符合规则的奖励在今天页显示为「领取本次奖励」卡片。
- 待领取卡片可使用成功背景和小型图标，但不能压过未完成任务列表。
- 奖励的领取、关闭和稍后处理都必须清楚可见，且不影响已完成记录。

### 4.6 我的页：备份、提醒与反馈

| 区块 | 规范 |
| --- | --- |
| 数据备份 | 柔白卡片中呈现导出、导入；导入前必须有明确的覆盖确认文案。 |
| 提醒可靠性 | 使用温和的琥珀色提示，不使用恐慌式错误样式；明确“不是闹钟级保证”。 |
| 问题反馈 | 设置行结构为图标 +「问题反馈」+「告诉我们你的体验与建议」+ 正式 SVG 箭头；高度至少 56px。 |
| 外部跳转 | 新标签页或系统浏览器打开飞书表单；产品内不嵌入复杂第三方界面。 |

## 5. 状态与反馈规则

| 场景 | 必须反馈 | 禁止做法 |
| --- | --- | --- |
| 保存任务 | 保存结果，并在“保存并开始”时直接衔接专注页 | 仅靠页面闪烁让用户猜是否成功。 |
| 每日首次完成 | 今日首次兑现状态变更；仅一次轻量庆祝 | 每次完成都重复播放强反馈。 |
| 领取奖励 | 明确已领取并移除待领取状态 | 自动弹出多层奖励选择。 |
| 导入数据 | 先警示覆盖范围，再确认执行 | 无确认直接覆盖现有本地数据。 |
| 提醒受限 | 显示能力边界及诊断信息 | 暗示可保证后台准点通知。 |
| 外部反馈 | 说明将打开表单并保留用户离开当前页的预期 | 在 WebView 中强塞第三方管理界面。 |

## 6. 文案原则

- 使用具体、可行动的词：如“创建第一件任务”“保存并开始”“完成任务”。
- 描述用户处境而非系统分类：如“遇到突发情况”“今天无法继续”。
- 不强制用户解释失败，不用羞耻、惩罚或比较型措辞。
- 隐私相关文案应说明“可选”“仅用于联系回复”“导入会覆盖本地数据”等实际结果。

## 7. 视觉验收清单

- [ ] 390 × 844 下引导、今天页、新建任务、专注页、完成后今天页、我的页均无溢出。
- [ ] 主按钮在一个关键操作区只有一个；次要/危险操作层级明确。
- [ ] 设置行、快捷时长和图标按钮均达到 44px 可触达范围；设置行至少 56px 高。
- [ ] 键盘 Tab 焦点、对话框焦点和 `aria-label` 可用。
- [ ] 完成任务后没有自动奖励弹窗或多重反馈叠加。
- [ ] 提醒、导入与外部反馈跳转的限制说明清楚且不造成视觉恐慌。
- [ ] 未引入深色、赛博、强游戏化或 SaaS 仪表盘视觉语言。
