# 今日兑现 v1.1｜数据库迁移设计

状态：设计完成，待 Supabase Free 项目创建后执行  
日期：2026-08-29  
适用范围：Supabase Auth + PostgreSQL，v1.1 Beta

## 1. 设计目标

在不破坏 v1.0 本地离线数据结构的前提下，为每位登录用户提供一份受 RLS 保护的云端状态快照。v1.1 优先解决账户、迁移和恢复；不提前为文件上传或复杂协作建立无用表结构。

## 2. 迁移顺序

| 顺序 | 名称 | 内容 |
| --- | --- | --- |
| `0001` | `profiles_and_snapshots` | 创建资料与用户快照表、更新时间函数、索引。 |
| `0002` | `enable_rls_and_policies` | 启用 RLS，撤销匿名访问，建立仅限本人读写策略。 |
| `0003` | `auth_profile_trigger` | Auth 用户创建后自动生成资料行。 |
| `0004` | `schema_guardrails` | 快照结构、版本和大小校验；为后续迁移预留约束。 |

实际 SQL 必须通过 Supabase CLI 或 SQL Editor 执行，并作为独立迁移文件保存。执行前应先在空 Free 项目验证；不得在生产数据上直接试错。

## 3. 逻辑数据模型

### 3.1 `public.profiles`

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| `id` | `uuid` | 主键，外键关联 `auth.users(id)`，删除时级联。 |
| `display_name` | `text` | 可空，最长 50 字符；仅用于未来展示。 |
| `created_at` | `timestamptz` | 默认当前时间。 |
| `updated_at` | `timestamptz` | 每次更新自动刷新。 |

### 3.2 `public.user_snapshots`

| 字段 | 类型 | 规则 |
| --- | --- | --- |
| `user_id` | `uuid` | 主键，外键关联 `auth.users(id)`，一位用户一份当前快照。 |
| `state` | `jsonb` | 非空；保存 v1.0 的任务、事件、奖励、设置等状态对象。 |
| `schema_version` | `integer` | 当前为 `1`，用于后续兼容迁移。 |
| `revision` | `bigint` | 从 0 开始递增，用于检测本地/云端版本冲突。 |
| `updated_at` | `timestamptz` | 云端最后成功写入时间。 |
| `created_at` | `timestamptz` | 首次创建时间。 |

`state` 的 v1.1 最小合法结构：

```json
{
  "tasks": [],
  "events": [],
  "rewards": [],
  "punishments": [],
  "settings": {}
}
```

客户端上传前与数据库写入前均校验最小结构；超出允许大小、缺少 `tasks` 数组或版本非法时拒绝写入。

## 4. RLS 权限矩阵

| 资源 | 匿名用户 `anon` | 登录用户 `authenticated` | 服务端 `service_role` |
| --- | --- | --- | --- |
| `profiles` | 无访问 | 仅 `id = auth.uid()` 的增查改 | 仅服务端维护操作 |
| `user_snapshots` | 无访问 | 仅 `user_id = auth.uid()` 的增查改 | 仅服务端故障恢复/运维 |

必须执行的原则：

1. 所有 `public` 业务表均启用 RLS。
2. 创建策略前先撤销不必要的 `anon` 权限。
3. 策略同时限定 `USING` 与 `WITH CHECK`，防止用户通过更新把 `user_id` 改为他人 ID。
4. 不在浏览器使用 `service_role`；该密钥可绕过 RLS。
5. API 暴露前，用两个独立测试账号执行跨用户读取、插入、更新、删除的拒绝测试。

## 5. 迁移 SQL 轮廓

以下为设计轮廓，不含项目密钥或生产配置；实际执行前需根据 Supabase 项目生成独立 SQL 迁移。

```sql
create table public.profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  display_name text check (char_length(display_name) <= 50),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table public.user_snapshots (
  user_id uuid primary key references auth.users(id) on delete cascade,
  state jsonb not null,
  schema_version integer not null default 1 check (schema_version >= 1),
  revision bigint not null default 0 check (revision >= 0),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint user_snapshots_state_has_tasks check (jsonb_typeof(state->'tasks') = 'array')
);

alter table public.profiles enable row level security;
alter table public.user_snapshots enable row level security;

create policy "users manage own profile"
on public.profiles for all to authenticated
using (id = auth.uid()) with check (id = auth.uid());

create policy "users manage own snapshot"
on public.user_snapshots for all to authenticated
using (user_id = auth.uid()) with check (user_id = auth.uid());
```

`profiles` 的插入由 `auth.users` 创建触发器完成，避免客户端伪造他人资料。`user_snapshots` 由登录后的客户端在 RLS 条件内创建或更新。

## 6. 客户端同步接口契约

| 操作 | 前置条件 | 输入 | 成功结果 | 失败处理 |
| --- | --- | --- | --- | --- |
| `loadCloudSnapshot` | 已登录 | 当前用户 ID | 云端状态、版本、更新时间 | 保持本地数据并提示重试。 |
| `createCloudSnapshot` | 云端为空 | 本地状态、版本 | 创建快照 | 冲突时重新读取云端。 |
| `updateCloudSnapshot` | 已登录且版本可用 | 状态、预期版本 | 版本递增 | 版本不一致时进入用户选择流程。 |
| `restoreCloudSnapshot` | 用户已确认 | 云端状态 | 覆盖本地内存并持久化 | 恢复前保留本地 JSON 备份提示。 |

客户端不直接拼接 SQL，不使用数据库密码；仅通过 Supabase JavaScript SDK 访问被 RLS 保护的表。

## 7. 认证与回调配置

创建项目后配置：

- Site URL：`https://today-fulfillment.netlify.app`
- 开发 URL：本地开发服务器地址，仅用于开发环境
- Redirect URLs：注册验证、密码重设和开发地址对应回调
- Auth provider：仅开启 Email；关闭 Phone、匿名登录和未使用的 OAuth
- Confirm Email：开启
- SMTP：仅开发测试阶段使用默认能力和授权测试邮箱；公开用户邮件配置需另行批准

## 8. 环境变量与敏感信息

| 配置 | 可出现位置 | 禁止出现位置 |
| --- | --- | --- |
| Supabase Project URL | 前端运行时配置，可公开 | 无需作为密钥保护。 |
| Publishable/anon key | 前端运行时配置，必须依赖 RLS | 不可被误认为服务端高权限密钥。 |
| `service_role` | 仅 Netlify/Supabase 服务端机密配置 | 浏览器、Git、文档、测试输出、51.LA。 |
| SMTP 密码 | 仅 Supabase Auth SMTP 设置 | 前端、Git、Netlify 公开变量。 |
| 数据库密码 | Supabase Dashboard / 受控开发环境 | 所有代码仓库与前端。 |

提交前检查：`.env`、`.env.*`、Supabase 本地凭据、导出数据和 SMTP 凭据必须被 `.gitignore` 排除；仅提交 `.env.example` 的无敏感字段名。

## 9. 验证与回滚

### 验证

- 空项目执行迁移无错误；
- 新建两个测试账户，资料行自动创建；
- 每个账户只可访问自身的资料和快照；
- 未登录请求被拒绝；
- 上传、更新、版本冲突、恢复、退出登录和删除账户后的级联删除行为符合预期；
- 快照数据库错误不影响本地任务继续使用。

### 回滚

- v1.1 Beta 期间不删除用户本地数据；
- 出现云端问题时，可关闭云同步入口并继续使用本地模式；
- 数据库变更使用追加迁移，不重写已执行迁移；
- 删除云端数据或项目属于高风险操作，必须单独获得用户确认。
