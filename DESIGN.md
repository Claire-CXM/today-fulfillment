# 今日兑现｜设计规范（MVP V3）

> 唯一视觉基线：用户确认的“图二”方案。项目参考图保存在 `docs/design-reference-v3.png`。
>
> 本版本覆盖此前“深青色横幅 + 通用白卡片”的实现。后续所有页面必须沿用本文件，不得再回退到图一风格。

## 1. 设计结论

设计名称仍为 **兑现花径**，但含义调整为“清透成长花园”。

核心体验不是效率仪表盘，而是一条有触感的成长路径：

- 大号中文标题建立轻松、坚定的产品气质。
- 清透浅色背景和柔光白色容器承载内容。
- 计划、专注、兑现由立体石子花径表达，并由真实任务状态驱动。
- 绿色代表进行中、完成和成长；浅蓝代表待开始；灰色代表未到达；黄色仅用于提醒；红色仅用于危险操作。
- 立体素材只出现在花径、成长徽章和低频完成反馈中，不使用金币、EXP、等级、人物或强游戏经济系统。
- 现有任务、计时、节点、奖惩、复盘、通知和本地数据规则全部保留，视觉不得改写业务含义。

## 2. 视觉特征

### 必须采用

- 近白底色，带非常轻的青绿与浅蓝空气感光晕。
- 页面标题使用 34–40px 的粗体中文，字面紧凑、有力量。
- 标题旁使用绿色手绘叶线作为品牌签名。
- 页面主体以白色、半透明感、24–30px 大圆角容器分组。
- 阴影柔和、扩散范围大、透明度低，形成悬浮而非厚重卡片感。
- 主按钮使用鲜明青绿；次按钮使用白底绿边；待开始标签使用浅蓝。
- 五栏底部导航使用线性图标，当前项为绿色实心或强化态。
- 今天页使用立体石子花径；阶段奖励使用植物徽章。

### 禁止采用

- 深青色大面积 Hero 横幅。
- 传统 SaaS 仪表盘、密集数据面板和深色统计卡。
- 每条任务各自成为厚重独立卡片。
- 虚线描边作为主要按钮风格。
- Emoji、文本符号或 CSS 拼画替代正式图标和视觉素材。
- 暖黄纸张、复古奶油色、金币、EXP、等级、角色、连击和高饱和金色。
- 为了装饰而增加与任务无关的步骤或动画。

## 3. 设计令牌

~~~css
:root {
  --bg-page: #F7FBF8;
  --bg-glow-green: rgba(116, 222, 184, .14);
  --bg-glow-blue: rgba(132, 190, 255, .10);
  --surface: rgba(255, 255, 255, .92);
  --surface-solid: #FFFFFF;
  --text-primary: #102F2B;
  --text-secondary: #667775;
  --brand: #17A879;
  --brand-strong: #087657;
  --brand-soft: #E8F8F0;
  --pending: #4E9EFF;
  --pending-soft: #EAF4FF;
  --line: #E4ECE8;
  --warning: #D19A21;
  --warning-soft: #FFF4D8;
  --danger: #D95B52;
  --danger-soft: #FFE9E5;
  --free-day: #7656B7;
  --shadow-card: 0 14px 38px rgba(38, 99, 79, .10);
  --shadow-float: 0 18px 46px rgba(38, 99, 79, .13);
}
~~~

- 正文对比度至少 4.5:1。
- 控件边界、焦点和状态标识至少 3:1。
- 状态不能只靠颜色，必须同时出现文字或图标。

## 4. 字体与排版

字体栈：

`"PingFang SC", "Microsoft YaHei", "Noto Sans SC", system-ui, sans-serif`

层级：

| 用途 | 手机字号 / 行高 | 字重 |
| --- | --- | --- |
| 产品主标题 | 36–40 / 1.08 | 800 |
| 页面标题 | 30–34 / 1.15 | 750 |
| 承诺文案 | 23–27 / 1.35 | 700 |
| 区块标题 | 21–24 / 1.3 | 700 |
| 任务标题 | 17–19 / 1.4 | 650 |
| 正文 | 14–16 / 1.65 | 400–500 |
| 辅助信息 | 12–13 / 1.5 | 400–500 |
| 倒计时 | 58–72 / 1 | 800 |

- 日期、任务数量和按钮不使用全大写英文装饰。
- 数字与倒计时使用等宽数字。
- 文案保持直接、平静、无羞辱。

## 5. 布局系统

设计基准为 390×844，最小宽度 320px。

- 手机内容区左右 22px；最小屏可降到 16px。
- 顶部安全区后先显示日期，再显示大标题和添加任务方形按钮。
- 区块间距 28–38px；容器内边距 18–24px。
- 大容器圆角 24–30px；按钮圆角 18–999px；输入框圆角 16–18px。
- 主触控目标不小于 44×44px。
- 页面底部必须预留底部导航高度与安全区，最后一个控件可完整滚动到导航上方。
- 600px 以上仍保持手机单列，最大宽度 720px，不改成桌面仪表盘。
- 弹窗宽度为 `min(calc(100% - 30px), 520px)`，最大高度 82vh，正文可滚动。

## 6. 今天页

今天页必须最接近参考图。

### 顶部

- 第一行显示本地日期，如“8月27日 周四”。
- 第二行显示大标题“今日兑现”和叶线素材。
- 右侧为 64×64 左右的添加任务方形按钮，图标为加号，文字“添加任务”。
- 不再显示英文眉题和日期胶囊。

### 今日承诺与花径

- 承诺区直接放在页面背景上，不使用深色卡片。
- 顺序为“今天的承诺”→“把一件事，做到完成。”→“今日完成 1/3”。
- 花径使用真实立体石子素材，下方显示“计划 / 专注 / 兑现”。
- 进度阶段由真实任务数据决定：
  - 无任务：计划未激活；
  - 有正式任务但未完成：计划完成、专注激活；
  - 当日全部完成：兑现激活。
- 花径是页面的核心视觉，不得缩成右侧小组件。

### 任务列表

- 区块标题左侧“今天要兑现什么？”，右侧显示“3项任务”。
- 所有任务放在一个白色大圆角容器中，以细分隔线分组。
- 单条任务结构：状态图标、标题与辅助信息、状态标签、主操作。
- 进行中任务可显示节点进度；无节点任务不显示进度条。
- 待开始为浅蓝状态；进行中与完成为绿色；失败/待补按业务颜色显示。
- 主操作使用圆角胶囊，不显示五六个同权重按钮。编辑、节点和危险操作进入低权重操作区，但功能必须保留。
- 空状态和建议任务仍可用，但也采用清透白色容器和图二的排版语言。

### 阶段奖励

- 任务列表下显示成长徽章、下一阶段文案和真实进度条。
- 不显示金币、账户余额、EXP 或等级。

## 7. 任务编辑与节点弹窗

- 独立子页隐藏全局大标题，使用返回按钮 + 页面标题。
- 表单使用一张清透白色大圆角容器。
- 标签置于字段上方；输入框高度至少 52px。
- 时长单位使用柔和分段控件。
- 节点编辑采用纵向步骤行，显示时长分配状态。
- 保存为唯一绿色主按钮；取消、规则拆分和添加节点为次操作。
- 键盘弹出时当前输入和提交按钮必须可见。

## 8. 专注页

- 独立子页只保留返回、任务标题和浮窗入口。
- 倒计时位于白色柔光主容器中央，字号 58–72px。
- 有节点时显示节点路线和完成状态；无节点时不显示任何可调进度。
- “完成任务/直接完成”为唯一主按钮。
- 普通暂停与特殊中断为次按钮；主动放弃为低权重红色文字按钮。
- 暂停额度、剩余次数和真实时间必须清晰可读。
- 动画只用于一次状态过渡；开启减少动画后即时切换。

## 9. 日历、复盘、奖惩、我的

所有主导航页面统一使用：

1. 日期或小型上下文；
2. 大号页面标题；
3. 1–2 个清透白色大圆角容器；
4. 真实状态颜色；
5. 线性图标和足够留白。

### 日历

- 月历置于单一白色大容器，日期按钮至少 44px。
- 状态文字统一为“全部完成、部分完成、未完成、自由日”。
- 日期详情和补做区使用独立柔光容器。
- 未来日期灰显，不能补做。

### 复盘

- 顶部用大号数字展示专注时长、任务进度等核心结果。
- 日总结和月总结使用分组容器，不使用深色统计卡。
- 优点、改进和鼓励按轻量信息层级呈现。

### 奖惩

- 首屏优先显示植物成长徽章与下一阶段目标。
- 奖励池和安全惩罚池分别使用白色容器和淡红容器。
- 领取记录、阶段进度和安全约束保持真实，不引入经济系统。

### 我的

- 顶部说明游客本地模式。
- 偏好与通知设置使用设置列表容器，行高至少 56px。
- 权限状态、最近检查和跳过原因用可读诊断卡表达。
- 危险或不可恢复说明使用红色，但不扩大视觉权重。

## 10. 底部导航与图标

- 固定五栏：今天、日历、复盘、奖惩、我的。
- 使用正式线性图标库；当前项使用绿色强化图标和文字。
- 导航为白色悬浮大圆角容器，含安全区；高度约 76–88px。
- 不使用字符“⌂、▦、◴、◇、●”作为图标。
- 专注和任务编辑仍是子页，不新增主导航项。
- 专注和任务编辑子页隐藏底部主导航，只保留返回与当前任务操作。

## 11. 视觉素材

项目素材：

- `assets/title-leaf-flourish.png`：产品标题叶线。
- `assets/journey-stones-v3.png`：三阶段立体花径。
- `assets/growth-badge.png`：阶段奖励植物徽章。
- `docs/design-reference-v3.png`：用户确认的图二视觉参考。

素材必须保持比例，不拉伸；透明素材不得置于冲突色背景。任务、导航和设置图标使用统一图标库，不用临时绘制的 CSS 图形。

## 12. 状态、反馈与无障碍

- 加载：保留现有内容，用局部骨架或按钮内状态。
- 成功：短 toast；任务完成使用植物成长徽章和柔白浮层，反馈总时长控制在约 1.2 秒，减少动画开启时直接跳过。
- 错误：说明事实与下一步，不使用空泛“操作失败”。
- 空状态：使用真实成长徽章、一句说明、一个主动作和三条建议任务，不使用文字字形或 CSS 拼图代替图像。
- 危险操作：主动放弃、删除未开始任务等继续二次确认。
- 弹窗：沿用柔白表面、30px 圆角、低强度遮罩和叶线标记；关闭与增删操作使用统一图标库，不使用“×”“+”等文字字形冒充图标。
- 键盘焦点使用 2px 品牌色外环与 2px 偏移。
- 支持系统字体放大、减少动画和触控操作。
- 返回、翻月、关闭等纯图标操作必须使用正式图标库并提供 aria-label；不得使用“‹”“›”“×”文字字形代替。
- 主要按钮、图标按钮和任务更多入口保持至少 44×44px 触控区域；紧凑标签中的移除按钮至少 32×32px。
- 所有页面在 320–430px 手机宽度下不得横向溢出，正文与弹窗内容可滚动到底且不被导航遮挡。

## 13. 实施优先级

1. 先建立全局令牌、标题、底部导航和清透容器。
2. 完成今天页的图二还原。
3. 统一任务编辑、节点、专注页。
4. 同步日历、复盘、奖惩、我的。
5. 在 390×844 下逐页截图，与 `docs/design-reference-v3.png` 同屏比较并修正。
6. 每次发布同步升级 PWA 资源版本，避免旧样式缓存。
