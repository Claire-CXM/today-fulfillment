# 今日兑现｜设计规范（MVP）

> 设计基线：**兑现花径**。它以“任务旅程”为叙事骨架，用清新、克制的轻游戏反馈帮助用户完成计划，而不是把学习变成数值收集。

## 1. 产品与设计目标

今日兑现帮助用户把当天真正重要的事计划出来、进入专注、如实记录结果，并获得可感知的反馈。

- 首屏先让用户看到“现在该做什么”，再看到进度与奖励。
- 游戏感只服务于承诺、进度和完成瞬间；不增加与核心闭环无关的认知负担。
- 用户始终掌控：能开始、暂停、特殊中断、补做和主动放弃，并能理解每一步的结果。
- 让状态、倒计时、数据持久化和反馈可靠可见；视觉不能掩盖任务本身。

## 2. 设计参考

| 参考 | 采用什么 | 不采用什么 |
| --- | --- | --- |
| 融合版「兑现花径」 | 顶部轻量花径、白色任务卡、薄荷绿主色、阶段奖励预告、任务优先的首屏 | 大面积插画和复杂数值面板 |
| A「青柠任务岛」 | “今日—任务—完成反馈”的旅程叙事与清新情绪 | 过重的岛屿场景和低龄化装饰 |
| 样式一「手绘清新」 | 叶片、短线等少量手绘签名，放在标题、空状态、奖励时刻 | 手绘边框用于按钮、输入框、进度等高频控件 |
| 样式二「清透渐变」 | 留白、卡片分组、低干扰渐变 | 过大的圆环和泛效率工具感 |
| 样式三「游戏成长」 | 阶段奖励的期待感与明确完成反馈 | 金币、EXP、等级、角色、连击等未经 PRD 确认的经济系统 |
| 场景化 PRD V2.0（飞书） | 五栏导航、计划—专注—复盘—奖惩闭环、状态边界 | 任何与既定业务规则冲突的视觉暗示 |
| Apple HIG 设计原则 | 目的感、能动性、熟悉感、简洁、匠心和愉悦；清晰反馈与可恢复操作 | 为了“好玩”而打断用户或隐藏后果 |
| Google Drive / Google Labs Code `design.md` | 作为后续工程化设计文档参考：清晰层级、可复用令牌、可实现的组件边界 | 未验证的外部视觉风格直接照搬 |

## 3. 整体视觉方向

**清新极简 × 轻游戏化 × 有节制的手绘温度。**

- 页面底色偏暖白；内容以白色卡片承载，主操作与正向状态使用深青绿和薄荷绿。
- “花径”是低高度的进度叙事：只说明今天的承诺和完成位置，不替代任务列表。
- 奖励以“快要解锁”的预告和一次完成庆祝来表达，不展示账户、货币或等级。
- 手绘元素仅做低频点睛：叶片、弧线、星点可出现于页标题、空状态、阶段奖励；不进入表单、导航、计时与状态标签。
- 文案直接、具体、少羞辱：按钮用动词，错误说明发生了什么与下一步能做什么。

## 4. 颜色、字体、布局和间距

### 颜色令牌

```css
:root {
  --bg-page: #F7F7EF;        --surface: #FFFEFA;
  --text-primary: #16352E;   --text-secondary: #658078;
  --brand: #176657;          --brand-strong: #10483D;
  --brand-soft: #DFF2E9;     --line: #D9DDD5;
  --success: #176657;        --warning: #C58B00;
  --danger: #D95B52;         --danger-soft: #FFE1DC;
  --reward: #F8C554;         --free-day: #7656B7;
}
```

- 绿色表示可行动、进行中、完成；黄色只表示待补、提醒或需注意；红色只表示失败/危险操作；自由日使用紫色。
- 状态必须同时有文字、图标或位置变化，不能只靠颜色。
- 正文与背景至少满足 4.5:1 对比度；控件边界与焦点至少 3:1。

### 字体与层级

- 字体栈：`Inter, "PingFang SC", "Microsoft YaHei", sans-serif`。
- 页面标题 28–32px / 700；区块标题 20–22px / 700；任务标题 17–18px / 700；正文 14–16px / 400–500；辅助信息 12–13px。
- 数字与倒计时使用等宽数字（`font-variant-numeric: tabular-nums`）；倒计时 48–56px。

### 布局与间距

- 以 4px 为基准；常用间距为 8 / 12 / 16 / 20 / 24 / 32px。
- 手机内容区左右 16–18px，区块间距 24–32px，卡片内边距 16–20px，卡片内元素间距 8–16px。
- 卡片圆角 18–20px；主 CTA 11–14px；胶囊标签为 999px。阴影只用于抬升层级，默认 `0 12px 30px rgba(23,61,51,.08)`。
- 首页顶部花径/承诺区应占首屏高度不超过约 28%；首屏必须露出至少两条任务及创建入口。

## 5. 核心组件

| 组件 | 规范 | 使用边界 |
| --- | --- | --- |
| 主按钮 | 44px 最小触达高度；深青绿底、白字；按下可稍深，禁用 48% 不透明度 | 每个操作区最多一个主按钮，如“开始专注”“保存任务”“完成任务” |
| 次按钮 / 描边按钮 | 薄荷浅底或透明描边；用于返回、暂停、换一批、稍后处理 | 不与主按钮争夺视觉优先级 |
| 危险按钮 | 浅红底红字；最终危险操作在确认弹窗中使用 | 主动放弃、确认惩罚等，必须配清晰后果说明 |
| 任务卡 | 标题、预计/剩余时间、状态标签、必要时节点预览与操作；运行中用绿边/柔和外环强调 | 顺序固定：进行中 → 暂停/中断/待开始 → 已结束 → 已完成；已完成在最底部 |
| 进度 | 仅拆分了节点的任务显示节点进度；无节点任务不显示可调的手动进度 | 进度条只表达实际完成，不能伪装为游戏经验条 |
| 状态标签 | 12px、圆角胶囊；文案使用“待开始、专注中、已暂停、待补、已结束、已完成”等 | 同时保持任务卡边框/说明与标签一致 |
| 输入与选择 | 高度 44px，1px 中性边框，聚焦为品牌色边框 + 2–3px 柔和外环 | 标签在输入框上方；错误信息紧邻字段下方 |
| 底部导航 | 固定五栏：今天、日历、复盘、奖惩、我的；当前项用浅薄荷背景和品牌色 | 专注、任务编辑为独立子页，不新增第六个主导航项 |
| 奖励预告 | 只显示下一阶段目标、当前累计与已解锁数量 | 不显示金币、EXP、等级、角色或连续打卡经济化表达 |

交互态优先级：`disabled > loading > active > focus > hover > default`。键盘焦点使用 2px 品牌色外环并保留 2px 偏移；减少动画开启后仅保留即时状态变化。

## 6. 加载、成功、错误和空状态

- **加载**：局部加载优先用骨架或按钮内小型转圈；不遮住已输入内容，不使用长时间全屏动画。超过 2 秒要说明正在处理什么。
- **成功**：保存、发布、领取奖励用短 toast；任务完成可出现一次 300ms 内的轻量彩屑/星点与明确结果。用户开启“减少动画”时直接展示结果。
- **错误**：先说明事实，再给一个可执行动作。例如“无法保存：浏览器存储不可用。请检查无痕模式或可用空间。”不使用“操作失败”“再试一次”等空泛文案。
- **空状态**：使用一个简洁图形、1 句价值说明、1 个主动作。今日无任务时提供三条建议任务与“换一批”，同时提供“新建任务”。
- **确认与恢复**：主动放弃、清空或不可逆奖惩必须二次确认；普通关闭/返回不弹确认。中断、补做、申辩须保留上下文与进度。

## 7. 手机端与响应式规则

- 设计基准为 390×844；支持最小 320px 宽度。全部主操作可单手触达，触控目标不小于 44×44px。
- `≤599px`：单列；底部导航贴底并包含安全区；页面底部预留 `导航高度 + env(safe-area-inset-bottom)`；正文绝不被固定导航遮挡。
- `≥600px`：内容最大宽度 680–720px，居中；底部导航悬浮为圆角条。任务与表单仍维持单列，避免把专注流程拆成桌面仪表盘。
- 弹窗在手机最大宽度为 `calc(100% - 30px)`、最大高度 82vh，内容区可滚动；键盘弹出时应保证当前输入与提交按钮可见。
- 文本允许系统字体放大；任务标题、状态、按钮不得截断关键语义。图标须有可访问名称，状态须有 `aria-label` 或等价文本。

## 8. 做什么？不做什么？

### 做什么

- 始终把当前任务、下一步动作与真实状态放在装饰之前。
- 用一致的绿色完成反馈、薄荷浅层级与少量手绘元素建立温和的“兑现感”。
- 用阶段奖励、节点完成和日历回顾形成长期反馈，但保持任务本身可读。
- 支持撤销、返回、二次确认与清晰错误，保护用户的时间、数据和心理安全。

### 不做什么

- 不引入金币、EXP、等级、角色、连续打卡等新经济系统，除非 PRD 单独确认。
- 不把花径、圆环、插画或动画做得比任务和按钮更显眼。
- 不为“游戏感”增加步骤、打断专注或用羞辱性文案催促用户。
- 不使用手绘风格替代控件边界、错误状态、按钮层级和无障碍反馈。
- 不在无节点任务中出现可调进度条；不改变既定五栏导航和任务状态排序。

## 参考链接

- [场景化 PRD V2.0（飞书）](https://pcngz2vyw6hl.feishu.cn/docx/IC1td5cg7osZspxpvAEcoPa0nVL?from=from_copylink)
- [Apple Human Interface Guidelines：设计原则](https://developer.apple.com/cn/design/human-interface-guidelines/design-principles)
- [Google Drive 设计参考](https://drive.google.com/file/d/1MZ0Qx68Vzw4x5x_XcV8XiPLp7fFDe1LJ/view)
- [Google Labs Code：design.md](https://zread.ai/google-labs-code/design.md)
