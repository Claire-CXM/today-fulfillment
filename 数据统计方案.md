# 今日兑现｜数据统计方案

## 1. 目标与核心口径

产品最重要的用户行为是：**完成一次兑现**，即用户将一项任务成功标记为完成（包括正常完成与补完成）。

核心指标：

> 每日完成至少 1 次兑现的活跃用户数 / 占比

当前没有账号体系，因此统计单位为 51.LA 识别的匿名浏览器/设备访客，而非真实自然人或账号。用户换设备、清除浏览器数据等情况可能被识别为不同访客；账号系统上线后再升级为账号级去重口径。

### 指标定义

| 指标 | 定义 |
| --- | --- |
| 每日兑现活跃用户数（DCAU） | 当天至少完成一次兑现的匿名去重访客数 |
| 每日兑现活跃用户占比 | DCAU ÷ 当日 51.LA 访客数（UV） |

分母已确定采用当日 51.LA UV。

## 2. 平台与安装方式

- 平台：51.LA 网站统计 V6。
- 生产域名：`today-fulfillment.netlify.app`。
- 51.LA 应用应开启“域名强匹配”。未来启用自定义域名时，需要在 51.LA 应用中同步补充该域名。
- 安装方式以用户提供的手动同步安装代码为基础。为保证关闭统计后重新打开产品时不会先产生一次上报，实际实现改为在读取本地隐私开关后按顺序加载 SDK 并初始化，而不是把外部脚本静态写死在 `<head>`。
- 初始化开启 51.LA 的事件分析能力，用于上报核心事件；不在本文档保存统计 ID 或完整初始化代码，避免不必要扩散。

## 3. 第一阶段事件设计

### 核心事件

`daily_fulfillment_achieved`

触发规则：任务状态已成功写入为“已完成”后触发。每个匿名浏览器/设备在本地日历日内仅首次完成任务时上报一次；当天继续完成其他任务不重复上报该事件。

正常完成与补完成都属于一次兑现，均纳入该事件。

### 可选分析维度

仅发送低敏感度、低基数的枚举或区间值：

- `completion_mode`：正常完成 / 补完成
- `source_view`：今天 / 专注页 / 日历
- `has_nodes`：是否设置节点
- `planned_duration_bucket`：计划时长区间
- `focus_duration_bucket`：专注时长区间
- `app_version`：产品构建版本

时长使用区间值，例如 0–30 分钟、31–60 分钟、61–120 分钟、120 分钟以上；不传送精确时间值。

## 4. 数据与隐私边界

允许发送：51.LA 的基础匿名访问统计与上述核心事件、枚举维度、时长区间。

禁止发送：任务标题、节点文本、任务 ID、反馈内容、联系方式、截图、申诉或中断文本、完整本地数据、精确行为时间戳，以及任何可直接识别用户身份的信息。

产品将在“我的”页增加说明与开关：

- 默认启用匿名使用数据统计；
- 用户可主动关闭“使用数据统计”；
- 关闭后不加载或不上报 51.LA 使用行为事件。

## 5. 实施与验证状态

已完成：

1. 增加独立统计适配层，集中管理加载与上报逻辑。
2. 根据本地隐私开关，以隐私优先方式加载 51.LA SDK 并按顺序初始化。
3. 开启官方事件分析模块，在任务完成状态的唯一提交点上报核心事件，并以本地日历日保证首次完成只上报一次。
4. 在“我的”页增加匿名统计说明与开关，支持键盘焦点和移动端布局。
5. 仅当 51.LA 事件 SDK 实际接手事件后，才将当天记录为已上报；SDK 未就绪、加载失败或超时不会消耗当天的首次兑现机会。
5. 统计仅在正式生产域名启用，本地开发与测试环境不会加载 51.LA。
6. 已完成模块测试、业务回归测试、生产构建与 390 × 844 手机视口回归。

待部署后验证：在正式域名完成一次测试任务，等待 51.LA 数据处理后核对 UV、事件名称和事件维度是否正常入库。若首个事件未入库，请刷新到最新 PWA 版本并在无痕窗口重新测试；同一浏览器当天仅尝试上报首次兑现。未收到“上传”指令前不提交、不推送、不部署。

## 6. 容量提醒

51.LA 免费基础版的事件统计量有限。第一阶段每位访客每天至多发送一次核心事件，有利于控制消耗；如果后续达到平台免费额度，不启用任何付费扩容，先汇报实际使用情况并等待确认。
