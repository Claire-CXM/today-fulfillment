# 今日兑现 v1.0｜本轮迭代记录

更新日期：2026-08-29
版本状态：准备上线，等待推送至 `main` 后由 Netlify 自动部署

## 本轮目标

围绕「完成一次兑现」这一核心用户行为，降低第一次完成任务的理解与操作成本；同时补齐用户反馈、基础数据统计和本地数据安全能力，使产品具备 v1.0 上线条件。

## 已完成修改

| 范围 | 修改内容 | 用户价值 |
| --- | --- | --- |
| 首次体验 | 新增三步新手引导；仅对无历史数据的新用户展示，可跳过或直接创建第一件任务。 | 首次打开就知道产品如何开始。 |
| 今日页 | 新增「今日首次兑现」状态，完成首个任务后显示「已达成」。 | 明确当天最小成功目标。 |
| 新建任务 | 增加 15 / 25 / 45 / 60 分钟快捷时长；区分「只加入今天」与「保存并开始」。 | 减少输入和下一步犹豫。 |
| 任务拆分 | 将节点改为可选能力，补充适用条件与「阅读 → 练习 → 复盘」示例。 | 避免简单任务被迫拆分。 |
| 专注流程 | 主操作保留「完成任务」「短暂休息」；将突发情况、无法继续等低频处理收纳到「更多处理方式」。 | 关键路径更清晰，仍保留真实情境处理。 |
| 完成反馈 | 完成后先给出一次轻量庆祝，不自动弹出领奖；奖励改为今日页可稍后领取。 | 降低完成后的反馈叠加和打断。 |
| 提醒 | 补充提醒可靠性说明与诊断信息，明确浏览器/PWA 后台提醒并非闹钟级保证。 | 管理预期，减少误解。 |
| 我的 | 新增本地 JSON 导出、导入和覆盖确认；保留问题反馈入口。 | 用户可迁移和恢复本地数据。 |
| 用户反馈 | 「我的」设置列表保留“问题反馈”入口，外部打开飞书反馈表单；表单用于直接收集至飞书多维表格。 | 形成“产品入口 → 表单提交 → 后台处理”的反馈闭环。 |
| 数据统计 | 已接入 51.LA SDK，并在用户每日首次完成任务时上报 `daily_fulfillment_achieved`。 | 支持核心指标：每日完成至少 1 次兑现的活跃用户数/占比。 |
| PWA | Service Worker 缓存版本更新至 v29。 | 用户获得本轮资源更新。 |

## 验收结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 自动化测试 | 通过 | `npm test`：49/49 通过。 |
| 生产构建 | 通过 | `npm run build` 成功。 |
| 手机视口回归 | 通过 | 以 390 × 844 检查引导、新建任务、专注、完成反馈、我的页、备份与提醒说明。 |
| 核心闭环 | 通过 | 创建任务 → 保存并开始 → 完成任务 → 今日首次兑现达成 → 稍后领取奖励。 |
| 控制台检查 | 通过 | 上述核心页面与流程未发现控制台报错。 |
| 本地变更提交 | 已完成 | 本轮功能代码已形成独立本地提交：`2eee1b157a7b20754c253d35f110b2bfc9290517`。 |

## 未处理事项 / 上线前待办

1. 推送与部署：当前本地 `main` 比 `origin/main` 超前 1 个提交；此前连接 GitHub 失败，尚未推送，因此 Netlify 正式站尚未获得本轮代码。
2. 线上埋点验收：部署后在正式站完成一次任务，并在 51.LA 事件管理中核对 `daily_fulfillment_achieved` 是否入库。事件名称需与代码保持完全一致。
3. 反馈表单权限：上线前再用未登录或普通用户身份打开问题反馈入口，确认只能填写、不能编辑表单或查看记录；管理权限仅保留给运营者。
4. 数据导入提示：导入会覆盖当前设备的本地数据。正式使用前应先导出备份；本期不提供云端同步、账号合并或回收站。
5. 提醒边界：系统省电策略、浏览器限制或 PWA 未安装时，提醒可能延迟或漏发；不应将其作为强时效闹钟使用。

## v1.0 发布判定

满足以下条件后可发布：

- 功能提交已推送到仓库 `main`，Netlify 部署成功；
- 正式站能完成一次任务，且 51.LA 收到 `daily_fulfillment_achieved`；
- 飞书反馈入口以填写者权限可用，填写者无编辑和查看提交记录权限；
- 手机端完成核心闭环无阻塞问题。

## 本期未扩展范围

- 用户账号、登录和跨设备云同步；
- 推送通知或短信提醒；
- 基于 AI 的反馈自动分类；
- 奖励商城、多人协作与复杂报表。
