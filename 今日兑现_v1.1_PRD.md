# 今日兑现 v1.1｜产品需求文档（PRD）

版本：v1.1 Beta  
状态：已立项，待创建 Supabase Free 项目后实施  
日期：2026-08-29

## 1. 背景与目标

今日兑现 v1.0 的任务、奖励、设置和复盘数据仅保存在当前浏览器。本版本增加账户与云端数据能力，让用户在不破坏本地离线体验的前提下，通过登录恢复自己的数据。

### 本期目标

1. 用户可使用邮箱和密码注册、验证、登录、退出和找回密码。
2. 已有匿名用户首次登录后，可安全迁移当前设备的本地数据。
3. 登录用户可在新设备恢复其个人云端数据。
4. 所有云端个人数据都按用户隔离，不能被其他用户读取或修改。

### 成功指标

| 指标 | 定义 |
| --- | --- |
| 注册完成率 | 完成邮箱验证的用户数 / 发起注册的用户数。 |
| 数据迁移完成率 | 首次登录后成功完成本地数据迁移的用户数 / 有本地数据的首次登录用户数。 |
| 云端恢复成功率 | 新设备恢复成功的次数 / 发起恢复的次数。 |
| 数据安全底线 | RLS 安全测试中，不存在跨用户读取或写入。 |

## 2. 范围

### 2.1 本期包含

- 邮箱、密码、确认密码注册；
- 验证邮箱、邮箱密码登录、退出登录、忘记密码与重设密码；
- 账户状态、云同步状态展示；
- 本地数据首次迁移；
- 登录后的云端保存、恢复与离线待同步；
- 现有 LocalStorage、IndexedDB、JSON 导入导出继续可用；
- Supabase Auth、Database 与 RLS 权限。

### 2.2 本期不包含

- 手机验证码或第三方社交登录；
- 用户文件、任务附件、头像上传及 Supabase Storage；
- 多人共享、协作、管理员后台；
- 实时协作和自动化多端冲突合并；
- 飞书反馈数据迁移；
- 购买域名、SMTP 付费套餐、Supabase 付费套餐或任何付费扩展。

## 3. 用户故事与验收

| 用户故事 | 验收标准 |
| --- | --- |
| 作为新用户，我可以注册账户。 | 输入合法邮箱和合规密码后收到验证邮件；验证后可登录。 |
| 作为已注册用户，我可以安全登录。 | 正确邮箱密码登录成功；错误凭证不泄露账户是否存在等敏感细节。 |
| 作为忘记密码的用户，我可以重设密码。 | 重置链接只能使用一次，过期或无效时有明确提示。 |
| 作为 v1.0 老用户，我希望登录时不丢历史任务。 | 云端为空时，经确认可将本机数据迁移至云端。 |
| 作为换设备的用户，我希望找回数据。 | 新设备登录后可发现云端快照，并经确认恢复。 |
| 作为离线用户，我仍想继续完成任务。 | 离线时本地保存正常；联网后显示并完成待同步。 |
| 作为重视隐私的用户，我不希望别人看到数据。 | 任意已登录用户都无法读取、修改或删除他人的数据。 |

## 4. 账户流程

### 4.1 注册

1. 用户点击“注册”。
2. 输入邮箱、密码、确认密码，并同意必要的隐私提示。
3. 系统发送验证邮件，页面显示“请前往邮箱完成验证”。
4. 用户验证后回到今日兑现，自动建立会话或提示登录。

密码至少 8 位；前端仅做体验校验，真实身份认证、密码散列和会话由 Supabase Auth 处理。

### 4.2 登录与退出

- 登录字段：邮箱、密码。
- 登录成功后读取个人云端数据并进入数据选择流程。
- 退出时仅清除本设备会话，不删除本地或云端数据。
- 正常登录不发送邮件。

### 4.3 找回密码

1. 用户输入邮箱；
2. 系统显示统一成功提示，避免枚举账户；
3. 用户通过邮件链接进入重设密码页；
4. 新密码保存成功后回到产品。

## 5. 数据迁移与同步

### 5.1 首次登录决策

| 本地数据 | 云端数据 | 行为 |
| --- | --- | --- |
| 空 | 空 | 创建空云端快照。 |
| 有 | 空 | 展示“同步本机数据到云端”，用户确认后上传。 |
| 空 | 有 | 展示“恢复云端数据”，用户确认后恢复。 |
| 有 | 有 | 展示比较信息，用户选择“使用云端数据”或“保留本机数据并覆盖云端”。 |

禁止静默覆盖。覆盖云端前，产品必须再次说明后果，并建议先导出 JSON 备份。

### 5.2 同步策略

- 本地仍是即时写入与离线兜底；
- 登录后每次有效状态保存只记录最新的待同步版本，本机仍立即保存；
- 普通操作每个用户本地自然日最多自动上传一次；同日后续操作继续合并到待同步版本；
- 首次迁移、云端恢复、真实冲突选择、用户点击“立即同步”不受每日自动同步频次限制；
- 离线恢复网络时，仅在当日自动同步额度尚未使用时补传；否则保留待同步并允许手动同步；
- 云端保存 `revision` 与 `updated_at`；
- 只有本机与云端都相对同一同步基线发生不同修改时，才要求用户显式选择；选择完成后的普通操作不得重复弹出选择；
- 同步失败不阻塞创建任务、开始专注或完成兑现。

## 6. 页面与交互

| 页面/区域 | v1.1 变化 |
| --- | --- |
| 首次引导 | 增加“稍后登录”说明，不能阻挡匿名用户使用。 |
| 我的页 | 增加账户卡：未登录、待验证、已登录、同步中、同步失败、最近同步时间。 |
| 注册页 | 邮箱、密码、确认密码、验证邮件提示、返回登录入口。 |
| 登录页 | 邮箱、密码、忘记密码、注册入口。 |
| 数据选择对话框 | 清晰展示本机与云端状态，提供取消、恢复、覆盖等明确操作。 |
| 现有备份区 | 保留 JSON 导入/导出，并解释其与云同步的区别。 |

视觉继续使用 v1.0 的浅色青绿、柔白卡片和成长花径风格；账户能力不得使今天页的“完成一次兑现”主路径变复杂。

## 7. 安全、隐私与配置

- 云端只保存实现产品所需的个人任务数据和认证邮箱；
- 所有业务表均启用 RLS，策略以 `auth.uid()` 为数据所有者依据；
- 浏览器只能使用 Supabase Project URL 与 publishable/anon key；
- `service_role`、SMTP 凭据、数据库密码永不进入浏览器、Git 或 51.LA；
- 51.LA 继续只采集既有匿名行为事件，不发送邮箱、任务标题、任务内容或云端快照；
- 在真实公开注册前，应补充隐私说明，写明数据类别、用途、存储方、删除/导出路径。

## 8. 邮件服务与成本闸门

- 开发测试可使用 Supabase 默认邮件通道，仅面向项目所有者/团队授权邮箱；
- 对外注册前必须配置通过送达率测试的 SMTP；
- 只评估免费额度服务，任何购买、升级、绑定付款方式或自定义域名费用必须由用户单独确认；
- 未配置可公开使用 SMTP 前，注册入口只能作为开发测试，不得对外宣传可公开注册；
- 大陆邮箱验收至少覆盖 QQ、163、Gmail。

## 9. 发布验收

1. 注册、验证、登录、退出、密码重设的正常与异常路径通过。
2. 旧设备本地数据可上传至空云端账户；新设备可恢复。
3. 断网创建/完成任务后，恢复网络可同步。
4. 两个测试账号相互尝试读取、写入数据均被 RLS 拒绝。
5. `npm test`、`npm run build`、手机视口回归通过。
6. 提交前完成敏感文件检查；不上传 `.env`、服务端密钥、SMTP 凭据或数据库密码。

## 10. 上线判定

v1.1 Beta 仅在数据迁移、跨设备恢复、RLS 安全、离线回归和测试邮箱流程全部通过后上线。公开注册上线还需要用户单独确认 SMTP 服务、发信域名及可能产生的费用。
